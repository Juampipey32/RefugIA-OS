(refugia.sh is a shell script, which is source code - don't ignore)
(README.md is a markdown file, which is documentation/source - don't ignore)

(empty - no build artifacts, dependencies, or temp files to ignore)
