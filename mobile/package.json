{
  "name": "refugia-mobile",
  "version": "1.0.0",
  "description": "RefugIA — offline survival AI assistant (Android/iOS, on-device LLM)",
  "license": "MIT",
  "private": true,
  "scripts": {
    "prepare:www": "node scripts/build-www.js",
    "export:rag": "cd .. && python src/exportar_rag.py",
    "sync": "npm run prepare:www && npx cap sync",
    "add:android": "npx cap add android",
    "open:android": "npx cap open android",
    "build:apk": "npm run sync && cd android && ./gradlew assembleDebug",
    "build:apk:release": "npm run sync && cd android && ./gradlew assembleRelease"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/filesystem": "^6.0.0"
  }
}
